apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - nginx-deployment.yaml
  - api.yaml
  - svc.yaml

nameSuffix: -api

# patches:
#   - target:
#       kind: Service
#       name: svc
#     patch: |-
#       - op: replace
#         path: /spec/ports/0/name
#         value: http-port

patches:
  - patch: |-
      apiVersion: v1
      kind: Service
      metadata:
        labels:
          app: api-svc
        name: svc
      spec:
        ports:
        - name: http-port
          port: 8080
          protocol: TCP
          targetPort: 80
        selector:
          app: nginx-hello
        type: ClusterIP
